import { StyleSheet } from 'react-native'

export const CARD_REGISTER_SUCCESS_STYLES = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 25,
    gap: 22
  },
  headers: {
    gap: 10,
    alignItems: 'center',
  }
})