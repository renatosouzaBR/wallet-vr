import { StyleSheet } from 'react-native'

export const CARD_REGISTER_STYLES = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 25,
    gap: 22
  },
  rowGroupInputs: {
    flexDirection: 'row',
    gap: 12,
    width: '100%',
  }
})